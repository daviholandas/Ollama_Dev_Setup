version: '3.8'

services:
  architect-agent:
    image: vllm/vllm-openai:latest
    container_name: vllm-architect-agent
    runtime: nvidia
    environment:
      # GPU Configuration
      - NVIDIA_VISIBLE_DEVICES=0
      - CUDA_VISIBLE_DEVICES=0
      - VLLM_GPU_MEMORY_UTILIZATION=0.85
      
      # VLLM Core Features
      - VLLM_ENABLE_PREFIX_CACHING=1
      - VLLM_ENABLE_CHUNKED_PREFILL=1
      
      # Tool-Calling Features (NEW)
      - VLLM_ENABLE_AUTO_TOOL_CHOICE=1
      - VLLM_TOOL_CALL_PARSER=json
      - VLLM_MAX_PARALLEL_TOOL_CALLS=2
      - VLLM_TOOL_TIMEOUT=30
      - VLLM_ENABLE_SCHEMA_CACHE=1
      - VLLM_CACHE_TOOL_DEFINITIONS=1
      
      # Logging and Debugging
      - VLLM_LOG_LEVEL=INFO
    
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./tools:/app/tools
      - ./outputs:/outputs
      - ./logs/architect:/var/log/vllm
    
    ports:
      - "8000:8000"  # OpenAI-compatible API
      - "8100:8100"  # Metrics/monitoring port (optional)
    
    command: >
      --model Qwen/Qwen2.5-14B-Instruct-AWQ
      --served-model-name architect
      --gpu-memory-utilization 0.85
      --max-model-len 16384
      --tensor-parallel-size 1
      --enable-prefix-caching
      --enable-auto-tool-choice
      --tool-call-parser json
      --port 8000
    
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/v1/models"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    restart: unless-stopped
    
    labels:
      agent_name: "Architect Agent"
      agent_role: "System Design & Architecture"
      agent_port: "8000"
      tool_support: "true"

networks:
  default:
    name: vllm-network
    driver: bridge
